// import { request } from '../utils'

// We have two roles with codes 'ADM' for admin and 'SPADM' for super admin
// The only difference between both is super admin should be able to edit agreements

// the role of user is being returned in the response of the login query.

export default {
  isAuthenticated: true
}